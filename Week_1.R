# Basic operations 
# Assign value 5 to a variable
var_test = 5
# create a vector
hi_vec <- c(312.654, 192.6)
# Construct a matrix with 3 rows that contain the numbers 1 up to 7
matrix(1:7, byrow = TRUE, nrow = 3)
# Sex vector
sex_vector <- c("Male", "Female", "Female", "Male", "Male")
# Convert sex_vector to a factor
factor_sex_vector = factor(sex_vector)
# Check data type
class(my_numeric)
class(my_character)
class(my_logical)